import CalculatorClient from "@/components/CalculatorClient";

export default function OriginalPage() {
  return (
    <main className="mx-auto max-w-6xl px-4 py-8">
      <CalculatorClient mode="original" />
    </main>
  );
}
